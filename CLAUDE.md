# Working in this repo

- Never run `npm run dev` (or start/stop any dev server) yourself. Don't kill processes on Vite's ports either. The developer runs and restarts the dev server on their own machine — just make the code edits.
