rows inside groups, because they only have 1 option as delete so instead of 3 dots button, it should be tertiary trash icon button
make the chip input componet to have only 1 row of chip when chips overflow display badge (+N) for the remaining of items and on hover that N will display a tooltip with the remaining in a list
has any of has none of or has all of the fields next to them should be selection optiosn , currently when choose these conditions for clinical indication, the field is text input "enter value"

Make the component text input to have fixed height for the field: size text input : md 32 px, and large 40px

what's the dropdown component trigger from the condition field? is it dropdown menu or dropdown list? remove the padding top and bototm of each section inside 

The chip inside the chip input component differs to the chip used in pages/add-agreement-modal.jsx, it should wire from the library component 

If a criteria already selected, user start to choose condition, then change the criteria, the condition option selected should be kept. 

Add the section include: Text input "Name this search" and the button "Generate results" into the tab Basic search too

The button "Generate results" is disabled until: in basic: at least one criteria select, in advanced search: at least one field is 

